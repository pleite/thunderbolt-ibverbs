# kbuild outputs
*.ko
*.mod
*.mod.c
*.mod.o
*.o
.*.cmd
.Module.symvers.cmd
.modules.order.cmd
.tmp_versions/
Module.symvers
modules.order

# DKMS/local packaging outputs
*.deb
*.rpm
*.tar
*.tar.*
result
result-*

# Local benchmark raw artifacts. CSV/JSONL outputs live in each run's
# `result/` sibling and are picked up by the committed symlinks under
# `data/`. README per run documents the recreate command.
bench/results/**/result/
bench/results/**/*.json
bench/results/**/*.jsonl
bench/results/**/logs/

# Python bytecode
__pycache__/
*.pyc
